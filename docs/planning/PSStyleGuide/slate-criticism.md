# Criticism of the revised PSStyleGuide issue slate

## Review basis

This fixed-point review compares P1, P1A, P1B, P2, and P3 with the synchronized
T1, T1A, T1B, T2, T3, and T4 contracts. It assumes each repository executes
its one listed sequential graph and rechecks every finding from the prior
PSStyleGuide criticism after the cycle-2 revisions.

## Result

No actionable PSStyleGuide issue-slate finding remains.

The previous six findings are closed:

- P1 now produces named immutable `P1-SUPPLY-FREEZE-v1`; P1B requires exact
  manifest/lock/toolchain/tree/audit/decision equality at start and pre-merge.
- P1B names the Windows/writer diagnostic producers, exact paths, matrix-safe
  keys, byte/encoding limits, artifact names, conditions, and every upload
  input.
- P3 separates native `ObservedFindings` and package-keyed topology from human
  `Approvals` and validates keys, copied facts, topology, and governance as
  distinct phases.
- P3 physically allocates 48 Node, 16 manager, 36 staged/hook, 18 Husky
  installer, 15 live-capture, and 184 audit cases.
- P3 defines a recomputable npm-operation preimage and literal SHA-256
  `bacd645f3b9fd5e2b740d865c64488d6853935de23c05b07639d0c9f02d8dff1`.
- P3 closes the per-endpoint/whole-operation request budget, retryable
  statuses, `Retry-After`/rate-limit-reset precedence, 30-second cap, malformed
  header behavior, fixed fallbacks, safe evidence, and no-cache rule.

## Cross-repository convergence

The shared P/T layers now expose exact reciprocal comparison for:

- generated bytes, transactional per-artifact replacement, LF/BOM behavior,
  versions, supply inputs, Git identities, and path-set evidence;
- candidate archive/manifest/path/resource behavior, lifecycle and zero-call
  terminal cleanup, physical semantic cases, production outcomes, and harness
  verdicts;
- action/job/permission graphs, diagnostics, immutable artifact transport,
  terminal approval, sole writer, credentials, exact-lease publication,
  isolated real-ref evidence, and temporary/persistent rulesets; and
- finite Node/manager policy, literal package operations, installer/hook/audit/
  live-client physical cases, observed-versus-approved residual governance,
  read-only scheduled/manual validation, and Dependabot review-only state.

P2 versus T2/T4 product work, local case counts, staged versus full lint, and
Terraform state roles remain explicit intentional repository differences.

## Convergence judgment

This review is a no-finding fixed point for the PSStyleGuide slate. No issue
draft should be added, deleted, renamed, reordered, or revised from this
review.
