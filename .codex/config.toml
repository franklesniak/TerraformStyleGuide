# Preserve enough project-instruction capacity for the complete review workflow.
project_doc_max_bytes = 65536

[plugins."github@openai-curated"]
enabled = true

[features]
goals = true
multi_agent = true
